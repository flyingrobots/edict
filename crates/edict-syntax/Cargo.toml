[package]
name = "edict-syntax"
version = "0.3.0-alpha.1"
description = "Edict language lexer, parser, source AST, surface validation, initial Core lowering, and Core canonical encoding"
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
sha2 = "0.10.9"
