# Provider Fixtures

This directory contains provider manifest fixtures used by the provider topic
tests.

Current fixtures:

- `echo-generated/provider-manifest.json`: a representative provider manifest
  envelope with Echo-shaped coordinates. It proves generated artifact
  provenance, including the generic generation-provenance metadata role, and
  component role validation only. It is not an Echo semantics claim and does
  not imply provider loading or runtime execution.
